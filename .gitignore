# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
build/
dist/

# Envs
.venv/
.env
env/
venv/

# Notebook
.ipynb_checkpoints/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Project data — regeneratable from data.refresh, but the canonical
# parquet is committed so the deployed dashboard has data without a
# cold-start API fetch.
*.parquet
!smard_merged_15min.parquet
*.csv
!smard_merged_15min_head.csv
# Dashboard / notebooks read these at runtime — keep tracked so the deployed
# app and notebooks render correctly.
!backtest_results/lstm_weather_step7.csv
!backtest_results/ablation_summary.csv
!backtest_results/price_quantile_holdout.csv

# Model artifacts — only the deployed models are committed; experiments stay local.
model_checkpoints/*
!model_checkpoints/lstm_quantile_v1/
!model_checkpoints/price_quantile_v1/
mlruns/
models/production/
models/candidates/

# Training logs
logs/

# Internal planning — kept local
PLAN.md
design.md

# Outputs
output/
newplot.png
